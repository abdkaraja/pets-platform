---
phase: 2
slug: ecommerce-module
status: draft
shadcn_initialized: false
preset: not applicable — ASP.NET Core MVC + Tailwind CSS (not React)
created: 2026-07-19
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for the E-Commerce Module. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Tailwind CSS v4 utility classes — no component library) |
| Preset | not applicable |
| Component library | none — hand-built Razor partials + Tailwind utilities |
| Icon library | text emoji or inline SVG (no icon library dependency) |
| Font | System font stack (`font-sans`: system-ui, -apple-system, Segoe UI, sans-serif) |
| CSS framework | Tailwind CSS v4 via `@import "tailwindcss"` in `wwwroot/css/site.css` |
| JS framework | jQuery (slim, v3.x) + vanilla JS for Stripe.js integration |
| Layout engine | RTL (`<html lang="ar" dir="rtl">`) — all spacing uses logical properties |

### RTL Layout Contract (Critical)

The site renders in Arabic RTL direction. **All spacing must use Tailwind logical properties:**

| Directional Concept | Use Instead | Example |
|---------------------|-------------|---------|
| Left margin/padding | `ms-*` (margin-start) | `ms-2` not `ml-2` |
| Right margin/padding | `me-*` (margin-end) | `me-4` not `mr-4` |
| Text align left | `text-start` | `text-start` not `text-left` |
| Text align right | `text-end` | `text-end` not `text-right` |
| Float left | `float-start` | — |
| Border left | `border-s` | `border-s` not `border-l` |

**Exception:** `ml-auto` / `mr-auto` → use `ms-auto` / `me-auto`. Flexbox `justify-start`/`justify-end` are direction-safe.

---

## Spacing Scale

Standard Tailwind v4 default scale (matches existing Phase 1 usage):

| Token | Tailwind Class | Value | Usage |
|-------|---------------|-------|-------|
| xs | `p-1` / `m-1` / `gap-1` | 4px | Icon gaps, inline badge padding, tight spacing |
| sm | `p-2` / `m-2` / `gap-2` | 8px | Compact element spacing, input padding, card inner gaps |
| md | `p-4` / `m-4` / `gap-4` | 16px | Default element spacing, card padding, section gaps |
| lg | `p-6` / `m-6` / `gap-6` | 24px | Section padding, card-to-card gaps in grids |
| xl | `p-8` / `m-8` / `gap-8` | 32px | Major section breaks, page content padding |
| 2xl | `p-12` / `m-12` | 48px | Hero sections, major vertical breaks |
| 3xl | `p-16` / `m-16` | 64px | Page-level vertical spacing |

**Exceptions:**
- Cart item row vertical padding: `py-3` (12px) — compact list layout
- Product card image height: `h-48` (192px) — consistent image grid
- Product detail image: `h-64` to `h-96` (256–384px) — larger detail view
- Admin table row height: `py-2` (8px) — data-dense table rows
- Stripe Payment Element mount point: `min-h-[200px]` — Stripe requires minimum height

---

## Typography

| Role | Tailwind Class | Size | Weight | Line Height | Usage |
|------|---------------|------|--------|-------------|-------|
| Display | `text-3xl font-bold` | 30px | 700 | 1.2 | Hero headings (rare — page titles use Heading) |
| Heading | `text-2xl font-bold` | 24px | 700 | 1.3 | Page titles, section headings |
| Subheading | `text-xl font-semibold` | 20px | 600 | 1.4 | Card titles, subsection headings, product names |
| Body | `text-base` | 16px | 400 | 1.5 | Default text, descriptions, form labels |
| Body-small | `text-sm` | 14px | 400 | 1.5 | Secondary text, timestamps, metadata, table cells |
| Caption | `text-xs` | 12px | 400 | 1.4 | Badges, helper text, legal text, stock indicators |

**Font weights used:** 400 (regular), 600 (semibold), 700 (bold) — exactly 3.

**RTL text rules:**
- Headings: `text-start` alignment (RTL-aware)
- Price display: Always LTR (`dir="ltr"`) — currency formatting is LTR even in RTL pages
- Product names: Inherit RTL direction (Arabic text)
- Admin tables: `text-start` for all data cells

---

## Color

| Role | Tailwind Classes | Value | Usage |
|------|-----------------|-------|-------|
| Dominant (60%) | `bg-white` / `bg-gray-50` | `#ffffff` / `#f9fafb` | Page background, main content area |
| Secondary (30%) | `bg-gray-100` / `bg-gray-200` | `#f3f4f6` / `#e5e7eb` | Filter sidebar background, table alternating rows, input backgrounds, disabled states |
| Accent (10%) | `bg-blue-600` / `text-blue-600` / `border-blue-600` | `#2563eb` | **Reserved ONLY for:** primary CTA buttons, active filter chips, selected variant highlight, pagination active page, "Add to Cart" button, "Proceed to Checkout" button |
| Success | `bg-green-100 text-green-800` / `text-green-600` | `#16a34a` | In-stock indicator, "Order Delivered" badge, success toasts |
| Warning | `bg-yellow-100 text-yellow-800` / `text-yellow-600` | `#ca8a04` | Low stock warning, "Processing" status badge |
| Destructive | `bg-red-600` / `text-red-600` | `#dc2626` | Delete buttons, "Out of Stock" badge, error text, destructive confirmations |
| Neutral text | `text-gray-900` / `text-gray-600` / `text-gray-400` | `#111827` / `#4b5563` / `#9ca3af` | Primary text / secondary text / disabled/placeholder text |

**Accent reserved for (exhaustive list):**
1. Primary CTA buttons ("Add to Cart", "Proceed to Checkout", "Place Order")
2. Active/selected state indicators (filter chips, pagination, variant selection)
3. Navigation active state (current page in breadcrumb-like nav)
4. Links in body text (if styled as accent-colored)

**Status badge colors (Order Status per D-16, D-19):**

| Status | Badge Class | Text |
|--------|------------|------|
| Pending | `bg-yellow-100 text-yellow-800` | Pending |
| Processing | `bg-blue-100 text-blue-800` | Processing |
| Shipped | `bg-purple-100 text-purple-800` | Shipped |
| Delivered | `bg-green-100 text-green-800` | Delivered |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| **Primary CTA — Add to Cart** | "Add to Cart" |
| **Primary CTA — Checkout** | "Proceed to Checkout" |
| **Primary CTA — Place Order** | "Pay {amount}" (dynamic — e.g., "Pay $49.99") |
| **Empty state — Product catalog** | Heading: "No products found" / Body: "Try adjusting your search or filters, or browse all products." + "Clear Filters" link |
| **Empty state — Shopping cart** | Heading: "Your cart is empty" / Body: "Looks like you haven't added any products yet." + "Browse Products" link |
| **Empty state — Order history** | Heading: "No orders yet" / Body: "When you place an order, it will appear here." + "Start Shopping" link |
| **Empty state — Admin products** | Heading: "No products" / Body: "Create your first product to get started." + "Add Product" link |
| **Empty state — Admin categories** | Heading: "No categories" / Body: "Add a category to organize your products." + "Add Category" link |
| **Empty state — Admin inventory** | Heading: "No variants found" / Body: "Products must have variants to track inventory." |
| **Empty state — Admin orders** | Heading: "No orders" / Body: "Orders will appear here once customers start purchasing." |
| **Error state — General** | "Something went wrong. Please try again." + "Retry" button where applicable |
| **Error state — Add to cart failure** | "{problem description}" — e.g., "This item is out of stock." or "Only {N} items available. You have {M} in your cart." |
| **Error state — Checkout failure** | "Payment could not be processed. Please check your card details and try again." |
| **Error state — Network/server** | "Unable to connect. Please check your internet connection and try again." |
| **Destructive — Delete product** | "Delete Product": "Are you sure you want to delete '{name}'? This cannot be undone." |
| **Destructive — Remove from cart** | "Remove Item": "Remove '{name}' from your cart?" |
| **Destructive — Delete category** | "Delete Category": "Are you sure? Products in this category will need to be reassigned." |
| **Success — Added to cart** | "Added to cart" (brief toast/notification, auto-dismiss after 3 seconds) |
| **Success — Order placed** | "Order Confirmed!" / "Your order #{id} has been placed successfully." |
| **Success — Status updated** | "Order status updated to {status}" (admin notification) |
| **Stock warning** | "Only {N} left in stock" (shown when stock ≤ 5) |
| **Out of stock** | "Out of Stock" (badge on product card + button disabled on detail page) |
| **Price locked notice** | "Price at time of adding to cart" (shown on cart page for locked prices) |

---

## Page Layouts

### Layout Architecture

**Two layout zones:**

1. **Public/Customer layout** — `_Layout.cshtml` (existing): navbar + container + footer
2. **Admin layout** — `_AdminLayout.cshtml` (new): sidebar nav + content area

**Shared layout modifications to `_Layout.cshtml`:**

The existing layout gets nav items added:

```html
<!-- Add to existing <ul class="navbar-nav flex-grow-1"> -->
<li class="nav-item">
    <a class="nav-link text-dark" asp-controller="Catalog" asp-action="Index">Shop</a>
</li>
<!-- Cart icon with count badge — for authenticated users -->
<li class="nav-item">
    <a class="nav-link text-dark" asp-controller="Cart" asp-action="Index">
        Cart
        @if (cartItemCount > 0) {
            <span class="badge bg-blue-600 text-white text-xs rounded-full">{cartItemCount}</span>
        }
    </a>
</li>
<!-- Orders link — for authenticated users -->
<li class="nav-item">
    <a class="nav-link text-dark" asp-controller="Order" asp-action="Index">Orders</a>
</li>
```

**Admin layout (`_AdminLayout.cshtml`):**

```
┌──────────────────────────────────────────────────────────────┐
│ Top bar (same as _Layout.cshtml navbar)                      │
├──────────────┬───────────────────────────────────────────────┤
│              │                                               │
│   Sidebar    │           Content Area                        │
│              │                                               │
│  Products    │   @RenderBody()                               │
│  Categories  │                                               │
│  Inventory   │                                               │
│  Orders      │                                               │
│              │                                               │
└──────────────┴───────────────────────────────────────────────┘
```

- Sidebar: `w-64` (256px), `bg-gray-100`, `min-h-screen`, `p-4`
- Sidebar nav items: `block px-4 py-2 rounded hover:bg-gray-200 text-sm font-medium`
- Active sidebar item: `bg-blue-600 text-white`
- Content area: `flex-1 p-6`
- Responsive: sidebar collapses to top nav on mobile (`hidden md:block`)

---

### Page 1: Product Catalog (`Catalog/Index`)

**Route:** `/Catalog` or `/Catalog?search=...&category=...&petType=...&brand=...&minPrice=...&maxPrice=...&sort=...&page=1`

**Layout:** `_Layout.cshtml`

**Structure:**

```
┌──────────────────────────────────────────────────────────────┐
│ Page Title: "Shop" (text-2xl font-bold)                      │
├──────────────┬───────────────────────────────────────────────┤
│              │                                               │
│   Filters    │           Product Grid                        │
│   Sidebar    │                                               │
│   (240px)    │   ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│              │   │ Product │ │ Product │ │ Product │       │
│  Search      │   │  Card   │ │  Card   │ │  Card   │       │
│  ─────       │   └─────────┘ └─────────┘ └─────────┘       │
│  Pet Type    │   ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  ─────       │   │ Product │ │ Product │ │ Product │       │
│  Category    │   │  Card   │ │  Card   │ │  Card   │       │
│  ─────       │   └─────────┘ └─────────┘ └─────────┘       │
│  Price Range │                                               │
│  ─────       │           Pagination                          │
│  Brand       │   [1] [2] [3] ... [N]                        │
│              │                                               │
└──────────────┴───────────────────────────────────────────────┘
```

**Components:**

**Filter Sidebar (left, 240px, `bg-white rounded-lg shadow p-4`):**
- Search input: `<input type="search" placeholder="Search products..." class="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm">`
- Pet Type filter: Radio buttons or clickable chips — "All", "Dogs", "Cats", "Birds", "Fish", "Small Animals"
- Category filter: Checkboxes for categories (loaded from DB)
- Price Range: Two inputs (min/max) — `<input type="number" placeholder="Min" class="w-full border border-gray-300 rounded px-3 py-2 text-sm">`
- Brand filter: Checkboxes for brands (loaded from DB)
- "Clear Filters" link at bottom
- Filter state preserved in URL query params (server-side filtering, page reload)

**Product Card (`bg-white rounded-lg shadow hover:shadow-lg transition-shadow overflow-hidden`):**
- Image: `<img class="w-full h-48 object-cover">` or gray placeholder if no image
- Content padding: `p-4`
- Product name: `text-base font-semibold mb-1` (truncated to 2 lines: `line-clamp-2`)
- Category/Brand: `text-xs text-gray-500 mb-2`
- Price: `text-lg font-bold text-gray-900` — show computed price (base × multiplier for cheapest variant)
- Price range if variants differ: `text-lg font-bold text-gray-900` — show "{min} – {max}"
- Stock badge (if ≤ 5): `text-xs text-yellow-600` — "Only {N} left"
- Out of stock badge: `text-xs text-red-600 font-semibold` — "Out of Stock"
- Entire card is a link to `/Catalog/Details/{id}`

**Responsive:**
- Desktop (≥1024px): Sidebar + 3-column grid
- Tablet (768–1023px): Sidebar collapses to horizontal filter bar + 2-column grid
- Mobile (<768px): Filters collapse to "Filters" button that opens a slide-out panel + single column

**Sorting:**
- Dropdown above grid: `Sort by: Featured ▼` — options: Featured (default), Price: Low to High, Price: High to Low, Name: A–Z, Newest

**Pagination:**
- Below grid: `flex justify-center gap-2`
- Page buttons: `w-8 h-8 rounded border text-sm` — active: `bg-blue-600 text-white border-blue-600`
- Show: Previous / {1} {2} {3} ... / Next

---

### Page 2: Product Detail (`Catalog/Details`)

**Route:** `/Catalog/Details/{id}`

**Layout:** `_Layout.cshtml`

**Structure:**

```
┌──────────────────────────────────────────────────────────────┐
│ Breadcrumb: Shop > {Category} > {Product Name}               │
├──────────────────────────┬───────────────────────────────────┤
│                          │                                   │
│    Product Image         │    Product Info                   │
│                          │                                   │
│    ┌──────────────────┐  │    {Name} (text-2xl font-bold)   │
│    │                  │  │    {Category} > {Brand}           │
│    │   Main Image     │  │                                   │
│    │   (max-w-lg)     │  │    {Price} (text-2xl font-bold)  │
│    │                  │  │    or "From {min}" if variants    │
│    └──────────────────┘  │                                   │
│                          │    ─────                           │
│                          │    Description (text-gray-600)    │
│                          │                                   │
│                          │    ─────                           │
│                          │    Variants:                       │
│                          │    ┌──────────────────────┐       │
│                          │    │ Size: [S] [M] [L] [XL]│      │
│                          │    │ Color: [●] [●] [●]   │       │
│                          │    │ Weight: [1kg] [2kg]   │      │
│                          │    └──────────────────────┘       │
│                          │                                   │
│                          │    Stock: {N} in stock            │
│                          │    or "Out of Stock"              │
│                          │                                   │
│                          │    Quantity: [-] 1 [+]            │
│                          │                                   │
│                          │    [ Add to Cart ] (blue-600)     │
│                          │                                   │
└──────────────────────────┴───────────────────────────────────┘
```

**Components:**

**Image area:**
- Max width: `max-w-lg`
- Image: `w-full h-64 md:h-96 object-cover rounded-lg`
- No image: `bg-gray-200 rounded-lg h-64 flex items-center justify-center` with "No Image Available" text

**Variant selector:**
- Display as attribute groups: Size, Color, Weight (only show attributes that have values)
- Each group: label + horizontal button row
- Selected variant: `border-2 border-blue-600 bg-blue-50`
- Unselected variant: `border border-gray-300 hover:border-gray-400`
- Variant buttons: `px-3 py-1 rounded text-sm` — for color: add `w-8 h-8 rounded-full` with color swatch
- When variant is selected, show: price, stock level, SKU (if admin wants)
- If variant is out of stock: button is `opacity-50 cursor-not-allowed` with strikethrough text

**Quantity selector:**
- Button group: `[-]` `{qty}` `[+]`
- Min: 1, Max: stock level (or 0 if out of stock)
- `-` disabled at 1, `+` disabled at stock level
- jQuery-based increment/decrement with AJAX stock check

**Add to Cart button:**
- Full width: `w-full bg-blue-600 text-white py-3 rounded-lg font-semibold hover:bg-blue-700 transition-colors`
- Disabled state: `bg-gray-300 cursor-not-allowed` (out of stock)
- Loading state: "Adding..." with spinner
- Success: Brief toast "Added to cart!" (auto-dismiss 3s)

**Responsive:**
- Desktop (≥768px): Side-by-side image + info (flex layout)
- Mobile (<768px): Stacked — image full width, info below

---

### Page 3: Shopping Cart (`Cart/Index`)

**Route:** `/Cart`

**Layout:** `_Layout.cshtml`

**Structure:**

```
┌──────────────────────────────────────────────────────────────┐
│ Page Title: "Shopping Cart" (text-2xl font-bold)             │
├──────────────────────────────────────────┬───────────────────┤
│                                          │                   │
│   Cart Items (table/list)                │   Order Summary   │
│                                          │   ┌─────────────┐ │
│   ┌────────────────────────────────────┐ │   │ Subtotal:   │ │
│   │ [Image] Product Name               │ │   │ Shipping:   │ │
│   │         Variant: Size/Color        │ │   │ Tax:        │ │
│   │         Price: $XX.XX (locked)     │ │   │ ──────────  │ │
│   │         Qty: [-] 1 [+]  Remove    │ │   │ Total:      │ │
│   ├────────────────────────────────────┤ │   │             │ │
│   │ [Image] Product Name               │ │   │[Checkout]   │ │
│   │         ...                        │ │   │             │ │
│   └────────────────────────────────────┘ │   │             │ │
│                                          │   └─────────────┘ │
│                                          │                   │
└──────────────────────────────────────────┴───────────────────┘
```

**Cart Item Row:**
- Layout: `flex items-center gap-4 p-4 border-b border-gray-200`
- Image: `w-16 h-16 object-cover rounded` (or gray placeholder)
- Name: `font-semibold text-base`
- Variant details: `text-sm text-gray-500` — "Size: M, Color: Blue"
- Locked price: `text-sm text-gray-600` with tooltip: "Price at time of adding to cart"
- Quantity selector: Same as product detail (with stock validation)
- Remove button: `text-sm text-red-600 hover:text-red-800` — triggers confirmation modal

**Order Summary Card:**
- `bg-white rounded-lg shadow p-6 sticky top-4` (sticks on scroll)
- Subtotal: calculated from locked prices × quantities
- Shipping: Flat rate (agent's discretion — suggest $5.99 or free above threshold)
- Tax: placeholder "Calculated at checkout" (or $0.00 for v1)
- Divider: `border-t border-gray-200 my-4`
- Total: `text-xl font-bold`
- Checkout button: Same primary CTA style

**Empty cart state:**
- `bg-white rounded-lg shadow p-12 text-center`
- Icon: Cart emoji or SVG
- Heading: "Your cart is empty"
- Body: "Looks like you haven't added any products yet."
- Link: "Browse Products" button (blue-600)

**Responsive:**
- Desktop (≥768px): Side-by-side items + summary
- Mobile (<768px): Stacked — items first, summary below (sticky bottom bar on mobile)

---

### Page 4: Checkout (`Checkout/Index`)

**Route:** `/Checkout` (POST to create Stripe session, then render)

**Layout:** `_Layout.cshtml`

**Structure:**

```
┌──────────────────────────────────────────────────────────────┐
│ Page Title: "Checkout" (text-2xl font-bold)                  │
├──────────────────────────────────────────┬───────────────────┤
│                                          │                   │
│   Shipping Information                   │   Order Summary   │
│   ┌────────────────────────────────┐     │   (same as cart)  │
│   │ Full Name: [___________]       │     │                   │
│   │ Address:   [___________]       │     │   ┌─────────────┐ │
│   │ City:      [___________]       │     │   │ Items list  │ │
│   │ Phone:     [___________]       │     │   │ Subtotal    │ │
│   └────────────────────────────────┘     │   │ Shipping    │ │
│                                          │   │ Total       │ │
│   Payment                                 │   └─────────────┘ │
│   ┌────────────────────────────────┐     │                   │
│   │ ┌────────────────────────────┐ │     │                   │
│   │ │ Stripe Payment Element     │ │     │                   │
│   │ │ (card input renders here)  │ │     │                   │
│   │ │                            │ │     │                   │
│   │ └────────────────────────────┘ │     │                   │
│   │                                │     │                   │
│   │ [ Pay $49.99 ] (blue-600)     │     │                   │
│   │                                │     │                   │
│   │ ⚠ Error message area          │     │                   │
│   └────────────────────────────────┘     │                   │
│                                          │                   │
└──────────────────────────────────────────┴───────────────────┘
```

**Stripe Payment Element mount:**
- Container: `min-h-[200px]` (required by Stripe)
- Loaded from `https://js.stripe.com/dahlia/stripe.js`
- Appearance theme: `stripe` (matches blue accent)
- The Payment Element handles: card number, expiry, CVC, postal code — all within the embedded iframe

**Pay button:**
- Full width: `w-full bg-blue-600 text-white py-3 rounded-lg font-semibold hover:bg-blue-700`
- Shows dynamic amount: "Pay $49.99"
- Disabled while processing: "Processing..." with spinner
- Disabled on error until form is corrected

**Error display:**
- Below payment form: `bg-red-50 border border-red-200 rounded-lg p-4 text-red-600 text-sm`
- Stripe validation errors displayed inline

**Responsive:**
- Desktop (≥768px): Side-by-side form + summary
- Mobile (<768px): Stacked

**Security notes (for executor):**
- CSRF token required on form (ASP.NET default)
- Never pass secret key to client — only publishable key
- Price computed server-side from cart locked prices — never trust client

---

### Page 5: Order Confirmation (`Checkout/Confirmation`)

**Route:** `/Checkout/Confirmation?session_id={CHECKOUT_SESSION_ID}`

**Layout:** `_Layout.cshtml`

**Structure:**

```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│              ✓ Order Confirmed!                              │
│              (text-2xl font-bold text-green-600)             │
│                                                              │
│              Thank you for your order.                       │
│              Order #{id}                                      │
│                                                              │
│   ┌────────────────────────────────────────────────────┐     │
│   │ Order Details                                      │     │
│   │                                                    │     │
│   │ Status: [Pending] (yellow badge)                   │     │
│   │ Placed: {date}                                     │     │
│   │                                                    │     │
│   │ Items:                                             │     │
│   │   {Product Name} × {qty} ........ ${price}        │     │
│   │   {Product Name} × {qty} ........ ${price}        │     │
│   │                                                    │     │
│   │ Total: ${total}                                    │     │
│   │                                                    │     │
│   │ Shipping to: {address}                             │     │
│   └────────────────────────────────────────────────────┘     │
│                                                              │
│   [ View Order Details ]  [ Continue Shopping ]              │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**Note:** This page is shown after redirect from Stripe. The order is confirmed via webhook. If webhook hasn't processed yet, show: "Your order is being confirmed. You'll receive an email shortly." with a polling check or auto-refresh.

---

### Page 6: Order History (`Order/Index`)

**Route:** `/Order`

**Layout:** `_Layout.cshtml`

**Structure:**

```
┌──────────────────────────────────────────────────────────────┐
│ Page Title: "My Orders" (text-2xl font-bold)                │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌────────────────────────────────────────────────────┐     │
│   │ Order #{id}     {date}     [Pending]     $total   │     │
│   │ Order #{id}     {date}     [Shipped]     $total   │     │
│   │ Order #{id}     {date}     [Delivered]   $total   │     │
│   └────────────────────────────────────────────────────┘     │
│                                                              │
│   Each row is a link to /Order/Details/{id}                  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**Order Row:**
- `flex items-center justify-between p-4 border-b border-gray-200 hover:bg-gray-50`
- Order number: `font-semibold`
- Date: `text-sm text-gray-500`
- Status badge: colored badge per status table above
- Total: `font-semibold`
- Responsive: On mobile, stack vertically (number + date on first line, status + total on second)

**Empty state:** As defined in copywriting contract.

---

### Page 7: Order Detail (`Order/Details`)

**Route:** `/Order/Details/{id}`

**Layout:** `_Layout.cshtml`

**Structure:**

```
┌──────────────────────────────────────────────────────────────┐
│ Breadcrumb: Orders > Order #{id}                             │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   Order #{id}                    Status: [Pending] (badge)  │
│   Placed on {date}                                        │
│                                                              │
│   ─── Items ────────────────────────────────────────────     │
│                                                              │
│   [Image] {Product Name} - {Variant}                        │
│           {qty} × ${price} = ${subtotal}                    │
│                                                              │
│   Subtotal: ${subtotal}                                     │
│   Shipping: ${shipping}                                     │
│   Total:    ${total}                                        │
│                                                              │
│   ─── Status Timeline ──────────────────────────────────     │
│                                                              │
│   ● Pending ............ {date}                             │
│   ● Processing ......... {date}                             │
│   ○ Shipped                                              (future)
│   ○ Delivered                                            (future)
│                                                              │
│   ─── Shipping ─────────────────────────────────────────     │
│   {shipping address}                                        │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**Status Timeline component (per D-19):**
- Vertical timeline with dots and lines
- Completed status: `bg-blue-600` dot with solid line below
- Current status: `bg-blue-600` dot, bold text, date shown
- Future status: `bg-gray-300` dot, `text-gray-400` text, no date
- CSS: `border-s-2 border-blue-600` for completed line, `border-s-2 border-gray-200` for future

**RTL timeline:** The border-s (start) property handles RTL automatically — the line appears on the correct side.

---

### Page 8: Admin Products List (`Admin/Products/Index`)

**Route:** `/Admin/Products`

**Layout:** `_AdminLayout.cshtml`

**Structure:**

```
┌──────────────────────────────────────────────────────────────┐
│ Products                                      [+ Add Product]│
├──────────────────────────────────────────────────────────────┤
│ Search: [___________]    Filter: [All Categories ▼]          │
├──────────────────────────────────────────────────────────────┤
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ Name      │ Category  │ Brand   │ Price  │ Stock │ Actions│ │
│ ├───────────┼───────────┼─────────┼────────┼───────┼───────┤ │
│ │ Product A │ Dogs      │ BrandX  │ $29.99 │ 45    │ ✏ 🗑  │ │
│ │ Product B │ Cats      │ BrandY  │ $19.99 │ 0     │ ✏ 🗑  │ │
│ │ ...       │ ...       │ ...     │ ...    │ ...   │ ...   │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                                              │
│ Showing 1-10 of 45 products                                  │
│ [1] [2] [3] [4] [5]                                         │
└──────────────────────────────────────────────────────────────┘
```

**Table:**
- `w-full bg-white rounded-lg shadow overflow-hidden`
- Header: `bg-gray-50 text-sm font-semibold text-gray-600`
- Rows: `border-b border-gray-100 hover:bg-gray-50`
- Stock cell: colored — `text-red-600` if 0, `text-yellow-600` if ≤ 5, `text-gray-900` otherwise
- Actions: Edit (pencil icon) + Delete (trash icon, red)
- Product name links to edit page
- Pagination at bottom

---

### Page 9: Admin Product Create/Edit (`Admin/Products/Create`, `Admin/Products/Edit`)

**Route:** `/Admin/Products/Create` or `/Admin/Products/Edit/{id}`

**Layout:** `_AdminLayout.cshtml`

**Structure:**

```
┌──────────────────────────────────────────────────────────────┐
│ Breadcrumb: Products > {Create/Edit Product}                 │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌────────────────────────────────────────────────────┐     │
│   │ Basic Information                                  │     │
│   │                                                    │     │
│   │ Name:        [________________________]            │     │
│   │ Description: [________________________]            │     │
│   │                [________________________]            │     │
│   │ Category:    [Select Category      ▼]              │     │
│   │ Brand:       [Select Brand         ▼]              │     │
│   │ Pet Type:    [Select Pet Type      ▼]              │     │
│   │ Base Price:  [________] $                            │     │
│   │                                                    │     │
│   └────────────────────────────────────────────────────┘     │
│                                                              │
│   ┌────────────────────────────────────────────────────┐     │
│   │ Product Image                                      │     │
│   │ [  Upload Image  ]  (or drag & drop area)          │     │
│   │ Current: {filename} (if editing)                    │     │
│   └────────────────────────────────────────────────────┘     │
│                                                              │
│   ┌────────────────────────────────────────────────────┐     │
│   │ Variants                                           │     │
│   │ [+ Add Variant]                                    │     │
│   │                                                    │     │
│   │ ┌──────────────────────────────────────────────┐   │     │
│   │ │ Size: [___] Color: [___] Weight: [___]       │   │     │
│   │ │ Price Multiplier: [1.0]  Stock: [0]  SKU:[__]│   │     │
│   │ │ [Remove Variant]                              │   │     │
│   │ └──────────────────────────────────────────────┘   │     │
│   │ (repeats for each variant)                         │     │
│   └────────────────────────────────────────────────────┘     │
│                                                              │
│   [ Save Product ]  [ Cancel ]                               │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**Variant section:**
- Dynamic form rows added via jQuery (add/remove variant buttons)
- Each variant row: `bg-gray-50 rounded p-4 mb-3`
- "Add Variant" button: `text-blue-600 hover:text-blue-800 text-sm font-semibold`
- "Remove" button: `text-red-600 hover:text-red-800 text-sm`
- Minimum 0 variants allowed (product without variants shows base price only)

**Image upload:**
- Standard file input styled as drop zone
- Drop zone: `border-2 border-dashed border-gray-300 rounded-lg p-8 text-center hover:border-blue-400`
- Accepted: `.jpg`, `.jpeg`, `.png`, `.webp`
- Max size: 5MB
- Preview shown after selection

**Validation:**
- Client-side: jQuery validation (existing `_ValidationScriptsPartial`)
- Server-side: FluentValidation (existing pattern)
- Inline error messages: `text-red-600 text-sm mt-1`

---

### Page 10: Admin Categories (`Admin/Categories/Index`, `Admin/Categories/Edit`)

**Route:** `/Admin/Categories`

**Layout:** `_AdminLayout.cshtml`

**Structure (Index):**

```
┌──────────────────────────────────────────────────────────────┐
│ Categories                                  [+ Add Category] │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌────────────────────────────────────────────────────┐     │
│   │ Name              │ Parent         │ Products │ Actions│ │
│   ├───────────────────┼────────────────┼──────────┼───────┤ │
│   │ Dogs              │ —              │ 23       │ ✏ 🗑  │ │
│   │   Dry Food        │ Dogs           │ 8        │ ✏ 🗑  │ │
│   │   Wet Food        │ Dogs           │ 5        │ ✏ 🗑  │ │
│   │ Cats              │ —              │ 18       │ ✏ 🗑  │ │
│   │ Toys              │ —              │ 12       │ ✏ 🗑  │ │
│   └────────────────────────────────────────────────────┘     │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**Category tree display:**
- Indented rows for child categories (24px indent per level)
- `ps-{level * 6}` for RTL-aware indentation (start padding)
- Parent shown as plain text, children show parent name

**Category Edit (Create/Edit on same form):**
- Name input
- Parent category dropdown (optional — "None" for top-level)
- "Save Category" button
- If category has products: warning banner "This category contains {N} products. Deleting it will require reassigning products."

---

### Page 11: Admin Inventory (`Admin/Inventory/Index`)

**Route:** `/Admin/Inventory`

**Layout:** `_AdminLayout.cshtml`

**Structure:**

```
┌──────────────────────────────────────────────────────────────┐
│ Inventory Management                                         │
├──────────────────────────────────────────────────────────────┤
│ Search: [___________]    Filter: [In Stock ▼]                │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌────────────────────────────────────────────────────────┐ │
│   │ Product    │ Variant    │ SKU    │ Stock │ Status      │ │
│   ├────────────┼────────────┼────────┼───────┼─────────────┤ │
│   │ Dog Food   │ Large/Blue │ SKU-01 │ 45    │ In Stock    │ │
│   │ Dog Food   │ Small/Red  │ SKU-02 │ 3     │ Low Stock ⚠│ │
│   │ Cat Toy    │ —          │ SKU-03 │ 0     │ Out of Stock│ │
│   └────────────────────────────────────────────────────────┘ │
│                                                              │
│   Stock is editable inline — click stock number to edit,     │
│   press Enter to save.                                       │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**Inline stock editing:**
- Click on stock number → input appears (`w-20 border rounded px-2 py-1 text-sm`)
- Press Enter → AJAX save, input reverts to text
- Press Escape → cancel edit
- Invalid (negative) → error toast, revert

**Status column:**
- In Stock (≥6): `text-green-600` — "In Stock"
- Low Stock (1–5): `text-yellow-600` — "Low Stock" + warning icon
- Out of Stock (0): `text-red-600` — "Out of Stock"

---

### Page 12: Admin Orders List (`Admin/Orders/Index`)

**Route:** `/Admin/Orders`

**Layout:** `_AdminLayout.cshtml`

**Structure:**

```
┌──────────────────────────────────────────────────────────────┐
│ Orders                                                       │
├──────────────────────────────────────────────────────────────┤
│ Filter: [All Statuses ▼]    Search: [___________]            │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌────────────────────────────────────────────────────┐     │
│   │ Order #  │ Customer    │ Date    │ Total  │ Status  │     │
│   ├──────────┼─────────────┼─────────┼────────┼─────────┤     │
│   │ #1001    │ user@email  │ Jul 19  │ $49.99 │[Pending]│     │
│   │ #1002    │ user2@em    │ Jul 18  │ $29.99 │[Shipped]│     │
│   └────────────────────────────────────────────────────┘     │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

- Each row links to `/Admin/Orders/Details/{id}`
- Status column: colored badge (same as user-facing)
- Filter dropdown: All, Pending, Processing, Shipped, Delivered

---

### Page 13: Admin Order Detail (`Admin/Orders/Details`)

**Route:** `/Admin/Orders/Details/{id}`

**Layout:** `_AdminLayout.cshtml`

**Structure:**

```
┌──────────────────────────────────────────────────────────────┐
│ Order #{id}                                                  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   Customer: {email}          Date: {date}                   │
│   Status: [Pending] (current)                                │
│                                                              │
│   ─── Update Status ─────────────────────────────────────    │
│   [Processing ▼]  [ Update Status ]                          │
│   (dropdown shows next allowed status only — Pending→Processing)│
│                                                              │
│   ─── Items ────────────────────────────────────────────     │
│   {Product} - {Variant}  ×  {qty}  =  ${subtotal}          │
│                                                              │
│   Total: ${total}                                            │
│                                                              │
│   ─── Status Timeline ──────────────────────────────────     │
│   (same timeline component as user order detail)             │
│                                                              │
│   ─── Shipping ─────────────────────────────────────────     │
│   {address}                                                  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**Status update:**
- Dropdown shows only the next valid status (forward-only: Pending→Processing→Shipped→Delivered)
- "Update Status" button: `bg-blue-600 text-white px-4 py-2 rounded-lg`
- Confirmation: "Mark order as {status}?" (simple confirm, not full modal — low-risk admin action)
- After update: page reloads with success toast

---

## Interaction Patterns

### Add to Cart Flow

1. User clicks "Add to Cart" on product detail or product card
2. **If variant product:** Product card click → navigates to detail page (user must select variant first)
3. **If single-variant / no-variant product:** Product card has inline "Add to Cart" button
4. Button shows "Adding..." with spinner during AJAX POST
5. **Success:** Toast notification "Added to cart!" (auto-dismiss 3s), cart badge count updates
6. **Failure (out of stock):** Error message inline: "This item is out of stock."
7. **Failure (exceeds stock):** Error message: "Only {N} items available. You have {M} in your cart."

### Checkout Flow

1. User clicks "Proceed to Checkout" → navigates to `/Checkout`
2. User fills shipping info (name, address, city, phone)
3. Server creates Stripe Checkout Session with `ui_mode: "elements"`
4. Stripe Payment Element renders in the page (card form)
5. User enters card details, clicks "Pay ${amount}"
6. Stripe.js processes payment client-side (card data never touches server)
7. **On success:** Redirect to `/Checkout/Confirmation?session_id=...`
8. **On failure:** Error message below payment form, user can retry
9. Stripe webhook (`checkout.session.completed`) confirms payment server-side
10. If webhook is slow: Confirmation page shows "Your order is being confirmed..." with auto-refresh
11. After confirmation: Cart is cleared (per D-14)

### Search / Filter Flow

1. Filter changes trigger page reload with query params (server-side filtering)
2. Search input: debounced 300ms (jQuery) before submit
3. Filter state preserved in URL — shareable/bookmarkable
4. "Clear Filters" resets all filters to defaults
5. Results update with page reload (no AJAX for simplicity in v1)
6. Empty results: show "No products found" with "Clear Filters" link

### Quantity Update in Cart

1. User clicks +/- buttons
2. jQuery updates displayed quantity immediately (optimistic)
3. AJAX POST to update quantity on server
4. **If stock exceeded:** Server returns error, client reverts quantity, shows error
5. Cart total updates in order summary section

### Admin Status Update

1. Admin selects next status from dropdown
2. Clicks "Update Status"
3. Simple `confirm()` dialog: "Mark order as {status}?"
4. POST to server
5. Page reloads with success toast

---

## Responsive Breakpoints

| Breakpoint | Width | Layout |
|------------|-------|--------|
| Mobile | < 768px | Single column, stacked layout, hamburger nav, filters as slide-out |
| Tablet | 768–1023px | 2-column grid, sidebar filters become horizontal bar, admin sidebar hidden |
| Desktop | ≥ 1024px | Full layout — 3-column product grid, sidebar filters, admin sidebar visible |

**Key responsive rules:**
- Product catalog: 1 col → 2 col → 3 col grid
- Cart: Stacked → side-by-side with summary
- Checkout: Stacked → side-by-side with summary
- Admin tables: Horizontal scroll on mobile (`overflow-x-auto`)
- Admin sidebar: Hidden on mobile, toggle with hamburger icon
- Product detail: Stacked → side-by-side image + info

---

## Empty States

| Page | Heading | Body | CTA |
|------|---------|------|-----|
| Product Catalog | "No products found" | "Try adjusting your search or filters, or browse all products." | "Clear Filters" link |
| Shopping Cart | "Your cart is empty" | "Looks like you haven't added any products yet." | "Browse Products" → `/Catalog` |
| Order History | "No orders yet" | "When you place an order, it will appear here." | "Start Shopping" → `/Catalog` |
| Admin Products | "No products" | "Create your first product to get started." | "Add Product" → `/Admin/Products/Create` |
| Admin Categories | "No categories" | "Add a category to organize your products." | "Add Category" → `/Admin/Categories/Edit` |
| Admin Inventory | "No variants found" | "Products must have variants to track inventory." | (none — go create products) |
| Admin Orders | "No orders" | "Orders will appear here once customers start purchasing." | (none — wait for orders) |

**Empty state component:**
- `bg-white rounded-lg shadow p-12 text-center`
- Heading: `text-xl font-semibold text-gray-700 mb-2`
- Body: `text-gray-500 mb-6`
- CTA: `inline-block bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700`

---

## Error States

| Context | Display | Copy |
|---------|---------|------|
| Page load failure | Full-page error card | "Something went wrong. Please try again." + "Retry" button |
| Add to cart — out of stock | Inline toast/notification (red) | "This item is out of stock." |
| Add to cart — insufficient stock | Inline toast/notification (red) | "Only {N} items available. You have {M} in your cart." |
| Checkout — payment failed | Inline error below payment form | "Payment could not be processed. Please check your card details and try again." |
| Checkout — Stripe JS failed | Inline error | "Unable to load payment form. Please refresh the page." |
| Quantity update — stock exceeded | Inline toast (red) + revert quantity | "Only {N} items available." |
| Admin — delete failed | Inline toast (red) | "Could not delete. {reason}" |
| Network error | Toast (red, bottom-right) | "Unable to connect. Please check your internet connection and try again." |
| Form validation | Inline below each field | Specific validation message from FluentValidation |

**Error component:**
- Toast: `fixed bottom-4 end-4 bg-red-600 text-white px-6 py-3 rounded-lg shadow-lg z-50` — auto-dismiss 5s
- Inline: `bg-red-50 border border-red-200 rounded-lg p-4 text-red-600 text-sm`
- Field-level: `text-red-600 text-sm mt-1`

---

## UI Considerations

Applicable state considerations resolved: 14 covered, 2 backstop, 0 unresolved

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | Product catalog grid | ✅ covered | "No products found" empty state with clear-filters CTA |
| empty | Shopping cart list | ✅ covered | "Your cart is empty" with browse-products CTA |
| empty | Order history list | ✅ covered | "No orders yet" with start-shopping CTA |
| empty | Admin product/category/inventory/order lists | ✅ covered | Each has documented empty state with appropriate CTA |
| empty | Cart item list (single variant removed) | ✅ covered | Cart shows remaining items or switches to full empty state if last item removed |
| loading | Product catalog | ✅ covered | Page loads with skeleton: 6 gray placeholder cards (`bg-gray-200 rounded-lg h-64 animate-pulse`) |
| loading | Cart page | ✅ covered | Spinner centered while cart loads from DB |
| loading | Checkout (Stripe session creation) | ✅ covered | "Loading payment form..." with spinner while server creates Stripe session |
| loading | Add-to-cart button | ✅ covered | "Adding..." text + spinner replaces button content during AJAX |
| error | Add to cart (stock/existence) | ✅ covered | Inline error messages with specific copy per failure type |
| error | Checkout payment | ✅ covered | Error display area below Stripe Payment Element with retry guidance |
| error | Page load failure | ✅ covered | Full-page error card with retry button |
| populated | Product grid (typical) | 🧪 backstop | 3-column grid with cards, images, prices — visual verification needed |
| populated | Order timeline | 🧪 backstop | Vertical timeline with 4 status dots — visual verification needed |
| overflow | Product name (long) | ✅ covered | `line-clamp-2` truncation with ellipsis on product cards |
| overflow | Product description (long) | ✅ covered | `line-clamp-3` on detail page, full text on separate expandable section |
| long-text | Category name (admin table) | ✅ covered | Truncated with ellipsis at 200px width, full name on hover tooltip |
| zero-one-many | Product variants (0, 1, many) | ✅ covered | 0 variants: show base price only; 1 variant: show directly; many: show selector UI |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | N/A — no component library, no shadcn, no third-party UI registries | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
